# 集群部署改造清单

基于对项目的深入分析，以下是集群部署改造清单，按优先级排列。

---

## 🔴 P0 - 必须立即修复

### 1. Session 分布式存储（未实现）

- **位置**：全局缺失
- **后果**：用户登录后请求被路由到不同节点会丢失登录状态
- **方案**：引入 `spring-session-data-redis`,添加 `@EnableRedisHttpSession`；Shiro 需配置 Redis SessionManager

### 2. OTA 固件分片本地存储

- **位置**：`IotOtaServiceImpl.java:79` `device.ota.chunk-temp-dir:/tmp/iot-ota-chunks`
- **后果**：分片 1-5 被负载到不同节点的本地磁盘，合并时找不到所有分片
- **方案**：分片直接上传到 MinIO 临时 bucket，合并时从 MinIO 读取；或挂载共享存储 NFS

### 3. 短信限流本地 Map

- **位置**：`DySmsLimit.java:29-31` 用 `static ConcurrentHashMap` 存 IP 计数和黑名单
- **后果**：限流失效，同一 IP 实际可发 `5 × 节点数` 条，存在刷短信风险
- **方案**：迁移到 Redis，用 `incr + expire` 实现

---

## 🟠 P1 - 本周完成

### 4. `@Scheduled` 定时任务重复执行

- **位置**：
  - `QqWryDatHotReloadScheduler.java:18` 无分布式锁
  - `RedisServiceImpl.java:149` 监控记录无锁
  - `SignalingKeyScheduler.java:78` ✅ 已有锁（可参考）
- **方案**：统一参考 `SignalingKeyScheduler` 的 `setIfAbsent` 锁模式；或迁移到已有的 Quartz（`isClustered: true` 已配置好）/ XXL-Job

### 5. `@Cacheable` 本地缓存

- **位置**：12+ 个 Service（SysDepart、SysUser 等）
- **后果**：Node1 `@CacheEvict` 后，其他节点本地缓存仍脏
- **方案**：`application.yml` 添加 `spring.cache.type: redis`

### 6. Redis 监控指标本地 Map

- **位置**：`RedisServiceImpl.java:42` `static HashMap REDIS_METRICS`
- **方案**：迁到 Redis 存储 + 聚合查询

---

## 🟡 P2 - 本月完成

### 7. WebSocket 节流本地状态

- **位置**：`DeviceWebSocketServer.java:66-82` 节流 `latestMessages` 是 static
- **说明**：消息推送主通道已通过 Redis Pub/Sub 广播实现集群安全 ✅，但节流状态各节点独立
- **方案**：节流缓存迁到 Redis 短 TTL

### 8. 用户导入进度本地缓存

- **位置**：`ImportSysUserCache.java:13` static HashMap 存进度
- **后果**：导入发生在 Node1，前端轮询进度路由到 Node2 显示 0%
- **方案**：迁到 Redis，30 分钟 TTL

### 9. Quartz instanceId

- **位置**：`application-dev.yml` `instanceId: AUTO`
- **方案**：改为 `${HOSTNAME:node-${random.uuid}}` 防碰撞

### 10. MyBatis-Plus ID 策略

- **方案**：确认主键使用 `IdType.ASSIGN_ID`（雪花算法），避免数据库自增

---

## 🟢 P3 - 已基本实现，需验证

以下已具备集群能力，只需抽查验证：

| 项                         | 状态             | 验证点                                                                                                                                 |
| -------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| MQTT 共享订阅              | ✅ `$share` 前缀 | EMQX 侧配置一致                                                                                                                        |
| WebSocket 跨节点推送       | ✅ Redis Pub/Sub | `DeviceWebSocketServer.redisPublish`                                                                                                   |
| Pending Service 跨节点通知 | ✅               | `WebRtcAckPendingService`、`SlamCommandPendingService`、`DeviceCameraStreamPendingService` 都通过 Redis Pub/Sub + `RedisPendingListenerConfig` |
| Quartz 集群                | ✅               | `isClustered: true`, JDBC JobStore                                                                                                     |
| MDC 链路追踪跨线程         | ✅               | `TraceIdMdcFilter` + `AppConfig:69` TaskDecorator，但要确认无裸 `new Thread()`                                                         |
| Nacos 服务发现             | ✅               | `@EnableDiscoveryClient`，验证生产环境 `server-addr`                                                                                   |

---

## 部署层面补充

1. **Nginx / 负载均衡**：WebSocket 需开启 sticky session 或确认 Redis Pub/Sub 广播覆盖
2. **日志聚合**：已有 Loki / Zipkin，集群下务必带 `${HOSTNAME}` 区分节点
3. **EMQX**：建议集群部署，客户端连接串配多节点
4. **MinIO**：生产用分布式模式，避免单点
