# 生产环境：数据源 / Redis / mqtt / minio / darwin.integration 等由 Nacos realman-iot.yaml 管理。
# 勿在此重复上述配置，否则会覆盖 Nacos（见 application-test.yml 顶部注释）。

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.quartz.QuartzAutoConfiguration
